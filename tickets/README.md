# Tickets

This folder holds all ticket JSON files generated by the Requirements Agent.

Each file follows the naming convention `TICKET-XXX.json`.

## Ticket Status Flow

```
requirements_ready → code_ready → tests_passed → merged
                                → tests_failed → (back to dev)
```

## Do not manually edit these files.
They are managed by the agentic pipeline.
